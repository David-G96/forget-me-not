use rusqlite::Connection;

use crate::database::package_data::PackageData;

// pub fn get_all_package_data(conn: &Connection) -> Result<Vec<PackageData>, String> {
//     let mut stmt = conn.prepare("SELECT * FROM").map_err(|e| e.to_string());

// }
